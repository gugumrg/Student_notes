import 'package:flutter/material.dart';

class JumatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Jumat'),
      ),
      body: Center(
        child: Text('Halaman Jumat'),
      ),
    );
  }
}
