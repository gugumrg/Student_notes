import 'package:flutter/material.dart';

class SabtuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sabtu'),
      ),
      body: Center(
        child: Text('Halaman Sabtu'),
      ),
    );
  }
}
