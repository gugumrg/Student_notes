import 'package:flutter/material.dart';

class KamisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kamis'),
      ),
      body: Center(
        child: Text('Halaman Kamis'),
      ),
    );
  }
}
