import 'package:flutter/material.dart';

class SelasaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Selasa'),
      ),
      body: Center(
        child: Text('Halaman Selasa'),
      ),
    );
  }
}
