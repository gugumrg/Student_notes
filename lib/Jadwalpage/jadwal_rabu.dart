import 'package:flutter/material.dart';

class RabuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rabu'),
      ),
      body: Center(
        child: Text('Halaman Rabu'),
      ),
    );
  }
}
